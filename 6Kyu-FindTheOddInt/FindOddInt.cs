using System;


//  Problem: Given an array, find the integer that appears an odd number of times.
//  There will always be only one integer that appears an odd number of times.

//  Thought Process: This problem has two tasks. Count the number of times each value occurs, and list the value that is counted an odd number of times.
//                   In order to count each value in an array, I have to define each individual value.

    // TODO:
namespace _6Kyu_FindTheOddInt
{
    class FindOddInt
    {
    
        static int find_It(int[] seq)
        {
            int seq_size;
            for (int i = 0; i < length; i++)
            {

            }
            return result;
        }
        public static void Main()
        {
            int[] seq = { 20, 1, -1, 2, -2, 3, 3, 5, 5, 1, 2, 4, 20, 4, -1, 5, -2 };
            int n = seq.Length;
            Console.Write(find_It(seq));
        }
    
    }
}
